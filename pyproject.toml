[project]
name = "tnn"
version = "0.1.0"
description = "Tensor Neural Network implementation and examples"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["jupyter>=1.1.1", "matplotlib>=3.10.3", "numpy<2", "torch<2.3"]

[dependency-groups]
dev = ["ruff>=0.12.0"]
